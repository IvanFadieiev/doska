<center><h2>Eding a post:</h2>
<%= form_for([@user, @post]) do |f| %>
    <div class="field">
    <%= f.label :poster %><br />
    <%= f.text_field :poster %>
    </div>
    <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
    </div>
    <p>
    <%= f.submit "update" %>
  </p>
<% end %>

<%= link_to 'Back', post_path%>
</center>