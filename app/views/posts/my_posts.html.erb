 <center> <h1><strong><%= link_to current_user.user, current_user %> POSTS</strong><br></h1>
   <div class="round-image-300">
<%= image_tag(current_user.avatar.url(:medium)) %>
      </div>
<p>
_______________________________________
<% @post.each do |post| %>

<p>
  <strong>Title:</strong>
  <%= RedCloth.new(post.poster).to_html.html_safe %>
</p>

<p>
  <strong>Body:</strong>
  <%= RedCloth.new(post.body).to_html.html_safe %>
</p>
<td><%= link_to 'show', post_path(post) %></td><br>
_______________________________________
<% end %>
</center>